# Requirements for Liquidity Coupling simulation
# Python 3.10+ required (uses dataclasses, match statements)
# Zero external dependencies for core simulator and mechanism

# Optional: for the figures.ipynb notebook
matplotlib>=3.7.0
numpy>=1.24.0
jupyter>=1.0.0
